/** CS2's flash-blind duration cap in seconds — used to normalize `blind_duration` at export and to derive an active-blind window from `FlashVictimTuple`'s 0-1 `amt` for flash-impact stats. */
export const MAX_BLIND_DURATION_SEC = 6;
